#!/usr/bin/env bash
#MISE description="Rotate the oauth2-proxy client secret"
#MISE dir="{{config_root}}"
#MISE raw=true
#USAGE arg "<env>" help="Target environment (dev|test|prod)"

source IaC/_lib.sh
init_task "${usage_env:-}"
create_oauth2_secret "$ENVIRONMENT" --rotate
